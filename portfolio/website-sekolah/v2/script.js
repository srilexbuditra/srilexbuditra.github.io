const grid=document.querySelector('#grid');function card(x){return `<article class="card ${x.gradient}" data-category="${x.category}"><a href="detail.html?id=${x.id}"><div class="pic">${x.icon}<i>↗</i></div><div class="body"><label>${x.label}</label><h3>${x.title}</h3><p>${x.summary}</p><div class="meta">📅 ${x.date}<b>Lihat Detail →</b></div></div></a></article>`}grid.innerHTML=portfolioData.map(card).join('');document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');let f=b.dataset.filter;document.querySelectorAll('.card').forEach(c=>c.style.display=f==='all'||c.dataset.category===f?'block':'none')});const m=document.querySelector('.menu'),n=document.querySelector('nav');m.onclick=()=>n.classList.toggle('open');
