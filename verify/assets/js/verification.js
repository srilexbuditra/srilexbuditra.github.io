const params=new URLSearchParams(location.search);const id=(params.get('id')||'').trim();const $=s=>document.querySelector(s);
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
async function load(){const qr=$('#verifyQr');const qrLink=$('#qrLink');if(id){const u=location.origin+'/verify/?id='+encodeURIComponent(id);if(qr){qr.src='https://quickchart.io/qr?text='+encodeURIComponent(u)+'&size=240&margin=2';qr.alt='QR verifikasi '+id}if(qrLink)qrLink.href=u}
try{const res=await fetch('./data/documents.json',{cache:'no-store'});if(!res.ok)throw new Error('database');const db=await res.json();const doc=(db.documents||[]).find(x=>String(x.id).toUpperCase()===id.toUpperCase());if(!doc){showInvalid('Dokumen tidak ditemukan di database penerbit.');return}showValid(doc)}catch(e){showInvalid('Database verifikasi sedang tidak dapat diakses. Silakan coba lagi.')}}
function showValid(doc){$('#status').className='status valid';$('#statusText').textContent='DOKUMEN TERVERIFIKASI';$('#verifyMessage').textContent='Document ID ditemukan dan tercatat pada database penerbit.';const rows={id:doc.id,status:doc.status||'Verified',issued:doc.issued_at||'-',client:doc.client_name||'Terdaftar pada dokumen',project:doc.project||'-',fingerprint:doc.fingerprint||'-'};Object.entries(rows).forEach(([k,v])=>{const el=$('#v-'+k);if(el)el.textContent=v});$('#invalidNote').hidden=true}
function showInvalid(msg){$('#status').className='status invalid';$('#statusText').textContent='DOKUMEN BELUM TERVERIFIKASI';$('#verifyMessage').textContent=msg;$('#invalidNote').hidden=false;$('#invalidNote').textContent='Jangan menjadikan dokumen ini sebagai bukti penerbitan resmi sebelum Document ID terdaftar dan berstatus Verified.'}
load();