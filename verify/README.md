# Srilex Buditra Document Verification — V30

QR target: `/verify/?id=DOCUMENT_ID`.

Verification has two modes:
- Static GitHub Pages registry: `data/documents.json`.
- Optional automatic publisher API: see `V30_VERIFICATION_SETUP.md` and `worker/`.

Do not expose admin/database secrets in public JavaScript.
