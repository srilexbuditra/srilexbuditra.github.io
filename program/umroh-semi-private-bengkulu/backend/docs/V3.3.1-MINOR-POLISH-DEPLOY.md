# V3.3.1 Minor Polish — Deploy & Test

## Tidak ada migration D1 baru

### 1. GitHub
Commit message:

`Polish Umroh Staff Activation V3.3.1`

### 2. Worker
Deploy file:

`backend/worker/worker.js`

Target health:

```json
{"ok":true,"service":"umroh-auth-api","version":"3.3.1"}
```

Jangan ubah:
- DB
- AUTH_PEPPER
- ALLOWED_ORIGIN
- ENABLE_BOOTSTRAP=0
- SESSION_MAX_AGE_SECONDS

### 3. Verifikasi last_login_at
Patch ini memperbaiki aktivasi staf berikutnya agar `last_login_at` langsung terisi.

Untuk akun simulasi yang sudah diaktifkan sebelum V3.3.1, cukup:
1. logout dari Super Admin;
2. login normal sebagai `admin.simulasi`;
3. logout;
4. login kembali sebagai `srilexbuditra`.

Login normal memang sudah memperbarui `last_login_at`.

### 4. Verifikasi Manajemen Admin
Buka:

`/program/umroh-semi-private-bengkulu/admin/manajemen-admin/`

Target:
- `Admin Simulasi` menampilkan waktu **Login Terakhir**;
- audit aktivasi tampil sebagai `Admin Simulasi mengaktifkan akun`;
- tidak ada perubahan pada role/status akun.
